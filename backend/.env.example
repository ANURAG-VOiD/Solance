# WARNING: All placeholder values below are for local dev only.
# Every secret must be overridden in production.

PORT=8080
RUST_LOG=info
DATABASE_URL=postgres://solance:solance@localhost:5432/solance
# Generate with: openssl rand -hex 32
JWT_SECRET=dev-secret-change-in-production
# Production: https://your-domain.com — must match the browser origin exactly
FRONTEND_URL=http://localhost:3000
